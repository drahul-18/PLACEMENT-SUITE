/**
 * Primary Workspace — KodNest Premium Build System
 * 70% width, main product interaction area
 */

export function PrimaryWorkspace({ children }) {
  return <main className="primary-workspace">{children}</main>;
}
